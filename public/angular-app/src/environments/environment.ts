export const environment = {
  TOKEN_TYPE: "Token type. Example: bearer",
  DEFAULT_OFFSET: "Default offset for pagination. Example: 0",
  DEFAULT_COUNT: "Default count for pagination. Example: 5",
  BASE_URL: "Backend domaine name. Example: http://localhost:3000/api"
};
