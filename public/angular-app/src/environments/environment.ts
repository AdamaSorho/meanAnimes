export const environment = {
  TOKEN_TYPE: "Token type. Example: bearer",
  DEFAULT_OFFSET: "Default offset for pagination. It must be define as a string. Example: '0'",
  DEFAULT_COUNT: "Default count for pagination. It must be define as a string. Example: '5'",
  BASE_URL: "Backend domaine name. Example: http://localhost:3000/api"
};
